@code {
    [Inject] NavigationManager Navigation { get; set; }

    protected override void OnInitialized()
    {
        // Navigate to the login page when app starts or user is not authenticated
        Navigation.NavigateTo("/login");
    }
}
